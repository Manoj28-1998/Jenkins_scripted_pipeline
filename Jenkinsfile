node {
    stage('Checkout') {
        git 'https://github.com/your-username/your-repository.git'
    }
    stage('Test') {
        echo "Test is ready"
    }
    stage('Deploy') {
        echo "deployment is ready"
    }
}
